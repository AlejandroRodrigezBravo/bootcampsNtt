server:
    port: 8761      

eureka:
    instance:
        hostname: localhot
    client:
        registerWithEureka: false 
        fetchRegistry: false
        serviceUrl:
            defaultZone: httpa://${eureka.instance.hotsname}:${server.port}/eureka/   
